# --------------------------------------
# FUNCTION serve_meal
# required packages: none
# description:
# inputs:
# outputs:
########################################
serve_meal <- function(x=NULL,y=NULL){

# assign parameter defaults
if (is.null(x) | is.null(y)) {
  x <- runif(10)
  y <- runif(10)
}

# function body



return(message('...checking function: serve_meal()'))

} # end of function serve_meal
# --------------------------------------
# serve_meal()
