# --------------------------------------
# FUNCTION clean_up
# required packages: none
# description:
# inputs:
# outputs:
########################################
clean_up <- function(x=NULL,y=NULL){

# assign parameter defaults
if (is.null(x) | is.null(y)) {
  x <- runif(10)
  y <- runif(10)
}

# function body



return(message('...checking function: clean_up()'))

} # end of function clean_up
# --------------------------------------
# clean_up()
